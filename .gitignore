_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
.bundle/
.DS_Store

# Working source material — brochure PDF, design docs, Luke's hi-res originals.
# Kept local only; NOT published to the public repo or the built site.
source/

# R session history — never commit
.Rhistory
